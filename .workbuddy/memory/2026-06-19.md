# 2026-06-19 工作日志

## 代码改进建议梳理
- 通读 parser.ts、access.ts、bibtex-parser.ts、ris-parser.ts、3 个 formatter、ConvertPage 头部
- 给出按 P0/P1/P2 分级的改进清单，附优先级矩阵 SVG

### 关键证据
- **detectLanguage 三处一字不差**：parser.ts:257 / bibtex-parser.ts:79 / ris-parser.ts:72
- **authorStr 三处定义但行为不一致**：lsyj 用 ' and '、gbt7714 用 ', '、apa 用 ' & '/et al.
- **ID 生成器 5 处重复**：utils.ts:4 已有 generateId 但 parser/storage/bibtex/ris 各自重写，且 storage.ts:102 用 slice(2,6) 与其他 slice(2,7) 不一致
- **零测试**：Glob `**/*.test.{ts,tsx}` 无结果，package.json 无 vitest/jest
- **ConvertPage 1019 行 / HistoryPage 875 行**（wc -l 实测）
- **types.ts:77-79** Unicode 转义 `\u300A\u5386\u53F2...` 违反 CLAUDE.md 规范
- **access.ts:1** 邀请码 '1031' 硬编码进 bundle
- **BibTeX 嵌套花括号**：bibtex-parser.ts:14 正则处理不了 {A {B} C}
- **ConvertPage.tsx:50** forceUpdate 反模式；:58 opencc Converter 在组件体内重建

### 推荐执行路径
P0 (10min) → P2 去重 7/8/9 (30min) → P1 拆 ConvertPage (1-2h) → 补 parser 测试

## 生成《代码改进方案.md》
- 完整读了 access.ts、utils.ts、bibtex-parser.ts、lsyj.ts 头部、HistoryPage 头部
- 用 wc -l 实测各文件行数：ConvertPage 1019 / HistoryPage 875 / parser 450 / lsyj 367
- 输出文件：`C:\Users\31698\projects\yinyizhuan\代码改进方案.md`
- 文档结构：执行约定 → P0/P2/P1/P1可选 任务 → 执行顺序总览 → 全局验收清单 → 代码证据索引
- 每个任务含：位置（文件:行）、现状代码、改法（含代码示例）、验收清单（可勾选）
- 关键决策：authorStr 不强行合并（三格式规则本不同），只抽共同原语；ConvertPage 拆分分 5 步单独 commit；测试用 vitest + snapshot

## 在《代码改进方案.md》中新增 P3 章节
- 增加 P3 表单交互增强（3 项）：实时预览、输入校验、重置确认
- 把原 P0-2 的"重置确认"移到 P3-3，P0-2 只保留拖拽上传（避免重复）
- P3 标注依赖 P1-1（拆分 ConvertPage 后在 ManualForm 上做更干净）
- P3-1 实时预览：useMemo + formatCitation，空标题显示占位提示
- P3-2 输入校验：新建 src/lib/validate.ts，红框 + error message + 转换拦截
- P3-3 重置确认：进阶方案自定义弹窗 + 智能跳过（空表单不弹）
- 更新执行顺序总览（12 项）、全局验收清单、附录证据索引
